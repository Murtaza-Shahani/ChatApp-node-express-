<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write New Chat</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }
        .newchat {
            color: #333333;
            margin-bottom: 20px;
        }
        .form input[type="text"], .form textarea {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #cccccc;
            border-radius: 4px;
            box-sizing: border-box;
        }
        .form textarea {
            height: 100px;
            resize: none;
        }
        .create {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }
        .create:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <h3 class="newchat">WRITE Your New Chat here</h3>
        <form action="/chats" method="POST" class="form">
            <input type="text" placeholder="Who are U" name="from" class="input-text" required>
            <textarea name="message" placeholder="Write Ur msg" class="textarea" required></textarea>
            <input type="text" name="to" placeholder="add receiver" class="input-text" required>
            <button type="submit" class="create">Create Chat</button>
        </form>
    </div>
</body>
</html>
